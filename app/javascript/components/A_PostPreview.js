import React from "react"
import PropTypes from "prop-types"

const A_PostPreview = ({
image
}) => {
    return (
      <div className="preview">
        {image}
      </div>
    );
}

export default A_PostPreview
